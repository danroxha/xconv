package cli

const (
	COMMIT    = "commit"
	BUMP      = "bump"
	ROLLBACK  = "rollback"
	EXAMPLE   = "example"
	INIT      = "init"
	SCHEMA    = "schema"
	TAG       = "tag"
	CHANGELOG = "changelog"
	VERSION   = "version"
)
